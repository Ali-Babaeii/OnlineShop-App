import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: { flex: 1},
  headerContainer:{ flex: 1, justifyContent: 'center', padding: 10 },
  headerText:{ fontSize: 20, fontWeight: '600' }

});

export default styles;
